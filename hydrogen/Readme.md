# Setup

1. Install CMake
2. Generate the build, built it and run the executable

```sh
cd hydrogen
cmake -S . -B build
cmake --build build
./build/hydrogen
```


# ANNEX

## compile the cpp code
```
cd hydrogen
g++ src/main.cpp -o app
./app
```

